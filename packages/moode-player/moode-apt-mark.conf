# List of packages which should be frozen with apt-mark
alsa-cdsp
alsacap
# bluez
bluez-alsa-utils
# boss2-oled-p3
# bs2b-ladspa
camilladsp
camillagui
caps
# Specific v126 chromium package group
chromium-browser
chromium-browser-l10n
chromium-codecs-ffmpeg-extra
# Stock >v130 chromium package group
#chromium
#chromium-common
#chromium-l10n
#chromium-sandbox
#rpi-chromium-mods
#
# Rebuilt with the PCM meter scope fixes (alsa-project/alsa-lib #516 and #517),
# so the distro's own package must not replace it - a trixie point update to
# 1.2.14-1+deb13u1 already outranks 1.2.14-1moode1. On a base OS whose alsa-lib
# carries the fixes the recipe builds nothing, the package is not installed and
# these two lines are skipped; on one that does not, append the release that
# fixes it as a condition ("libasound2t64 << 1.2.17") to lift the freeze.
libasound2-data
libasound2t64
libasound2-plugin-bluez
librespot
mediainfo
minidlna
moode-player
mpd
mpd2cdspvolume
pleezer
python3-camilladsp
python3-camilladsp-plot
python3-libupnpp
python3-camilladsp
python3-camilladsp-plot
shairport-sync
shairport-sync-metadata-reader
squeezelite
trx
udisks
udisks-glue
upmpdcli
